<hr/>

<% @appointments.each do |appointment| %>
  id: <%= link_to appointment.id, controller: "appointment", action: "show", id: appointment.id %>
  name:<%= appointment.name %>
  status:<%= appointment.status %>
<% end %>
