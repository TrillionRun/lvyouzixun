<style>
.card-block {
  margin: 20px;
}
</style>
<div class="card card-block">
  <table class="table table-striped">
    <thead class="bg-primary">
      <tr>
        <th>用户ID</th>
        <th>邮箱</th>
        <th>姓名</th>
        <th>电话</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td>
            <%= link_to user.id, controller: "user", action: "show", user_id: user.id %>
          </td>
          <td><%= user.email %></td>
          <td><%= user.name %></td>
          <td><%= user.phone %></td>
          <td>
            <button type="button" class="btn btn-sm btn-outline-primary">
              <i class="fi-x" style="color: #d9534f"></i>
            </button>
          </td>
        </tr>
      <% end%>
    </tbody>
  </table>
</div>
